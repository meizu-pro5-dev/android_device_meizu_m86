# M86 radio lifecycle fragment. The platform rild service is defined by the
# Android 12 radio module; this fragment owns only the device reset trigger.

on property:sys.rild_reset=1
    stop vendor.ril-daemon
    setprop sys.rild_reset 0
    start vendor.ril-daemon
